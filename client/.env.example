# Client env - copy to .env.local for local dev
# Vite exposes env vars prefixed with VITE_ to the client.

# Local dev: Vite proxy forwards /api to server (see vite.config.js)
# Production: Set this to your deployed server URL in Vercel dashboard
VITE_API_URL=/api
