# Server env - copy to .env for local dev
# On Vercel: no env vars required for the basic API. Add below only if needed.

# Local dev only (Vercel sets PORT automatically for serverless)
PORT=3001

# Optional: Postgres (when you add a database)
# DATABASE_URL=postgresql://user:password@host:5432/dbname
